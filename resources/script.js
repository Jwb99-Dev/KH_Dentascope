/* KH Dentascope UK — Resources Page Script */
document.addEventListener('DOMContentLoaded', () => {
  // FAQ accordion is handled natively via <details>/<summary> elements
  // Add any resources-specific JS here
});
