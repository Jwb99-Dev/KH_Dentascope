/* KH Dentascope UK — Services Page Script */
// Page-specific JS for services page (currently minimal — global.js handles most functionality)
document.addEventListener('DOMContentLoaded', () => {
  // Any services-specific interactions can be added here
});
